    <!--------hero----------->
    <section class="hero py-16">
        <div class="container mx-auto flex items-center justify-between">
            <div class="w-1/2">
                <h6 class="text-lg"><em>Are you hungry?</em></h6>
                <h1 class="text-6xl font-bold mt-4">Don't wait !</h1>
                <button class="btn py-3 px-8 mt-2 rounded-full flex items-center font-bold">Order Now</button>
            </div>
            <div class="w-1/2">
                <img src="static/img/hero-pizza.png" alt="">
            </div>
        </div>
    </section>
    <!------------pizza------------>
    <section class="menu container mx-auto py-8">
        <h1 class="text-xl font-bold mb-8">All Pizzas</h1>
        <div class="grid grid-cols-4 gap-y-12 gap-x-16">
            <% pizzas.forEach(pizza=>{ %>
            <div class="w-64">
                <div>
                    <img class="h-40 mb-4 mx-auto" src="static/img/<%= pizza.image %> " alt="" srcset="">
                </div>
                <div class="text-center">
                    <h2 class="mb-4 text-lg"> <%= pizza.name %> </h2>
                    <span class="size py-1 px-4 rounded-full uppercase text-xs"><%= pizza.size %></span>
                    <div class="flex items-center justify-around mt-6">
                        <span class="font-bold text-lg">&#8377; <%= pizza.price %></span>
                        <button data-pizza="<%= JSON.stringify(pizza) %>" class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
                            <span>+</span>
                            <span class="ml-4">ADD</span>
                        </button>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    </section>
